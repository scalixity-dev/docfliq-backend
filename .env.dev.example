# .env.dev.example — commit this file (no real secrets here)
#
# How to use:
#   1. Copy to .env.dev:   cp .env.dev.example .env.dev
#   2. Fill in the values  (get them from your team lead / 1Password / Slack)
#   3. Run:                make setup-env
#      This reads .env.dev, fetches the RDS password, and writes .env
#
# .env.dev is gitignored — never commit it.

# Shared IAM user credentials (get from team lead)
AWS_ACCESS_KEY_ID=REPLACE_ME
AWS_SECRET_ACCESS_KEY=REPLACE_ME
